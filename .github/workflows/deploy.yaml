name: bastion host
on: push
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: checkout code 
        uses: actions/checkout@v2

      - name: deploy
        run: |
          echo "deploying to bastion host"
          echo "done"